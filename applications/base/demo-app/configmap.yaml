

apiVersion: v1
kind: ConfigMap
metadata:
  name: app-config
data:
  version: "v1.0.0"
  environment: "development"
  config.json: |
    {
      "app": {
        "name": "demo-app",
        "version": "v1.1.0",
        "features": {
          "logging": true,
          "metrics": true
        }
      }
    }